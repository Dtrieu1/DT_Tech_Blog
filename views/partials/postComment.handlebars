<div class="card border-dark mb-3" id="postCard">
    <div hidden id="postId">{{id}}</div>
    <div class="card-header">{{user.username}}: {{post_title}} </div>
    <div class="card-body text-dark">
        <p class="card-text postText">{{post_description}}</p>
    </div>
</div>